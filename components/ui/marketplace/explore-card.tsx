import { PropsWithoutRef

 } from "react";
const ExploreCard = (props:PropsWithoutRef) => {
        const {image, name, domain, contact} = props;
    return(
        <div>
            Explore Card
        </div>
    )
}

export default ExploreCard;