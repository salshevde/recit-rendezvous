/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['utfs.io','www.investopedia.com'],
    },
  };
  
  export default nextConfig;
  